<=.;

: [, , , , , , , , , , , ];
: [, , , , , , ];

   * {
    ?  ->? [, , , , , , ] {
        * ;
    }
    ?  ->? [, , , ] {
        * ;
    }
    ? () {
        * ;
    }
    * ;
}

  * {
    ?  ---  {
        * ;
    } ,, ?  ---  {
        * ;
    } ,, ? ( --- ) {
        * ;
    } ,, {
        * ;
    }
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<+>>;
    }
}

  * {
    ( ++ );
}

  * {
    ,.,  ++ ;
}

 ? * {
    : ;
    : ;
     <> @@;
    : @@@;
    : <<-..>>;
    : <<>> ++  + <<>> ++ ;
    *  - ;
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<>>;
    }
}

@  {
    => ? ? ? ? ? ? ? ? * {
         <>;
        ? ::  {
            ': @@@;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<..>>;
        } ,, {
             <> ;  <> ;
             <> ;  <> ;  <> ;  <> ;
             <> [, ];
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': [, , , , , , , <<>>, <<>>];
        }
        ? ' ::: [, ] {
            ': (
                ', ', ', ', ', ', '
            );
        } ,, {
            ': ';
        }
    }

    -  * {
        : '() - .();
        :  +++ ;
        : ;
        : ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        == ``  
        * ([, , , , ]);
    }

    ! * {
           * { * <-.((), , ); }
        *  --- {{
             -> ',
             -> (', ),
             -> (', ),
             -> (' + '<<>>, ),
             -> (' + '<<>>, ),
             -> (', ),
             -> (', )
        }};
    }

     * {
        :  +++ ;
        : ;

        : '<<..-..>>;
        : <-.(<<....->>);
        : ;

        : (', ');
        :  + (');

        ...  ->? [, , , , , , ] {
            ++: ;
            +: <<>>;
            : <<..>>;
        }

        -: '<<>> ++  ++  ++ ;
        -: '<<>> ++  ++ ;

        * ;
    }
}

@  {
    =>  * {
        ': ;
    }

    ! * {
           * { * <-.((), , ); }
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : ;
        ?  { +: ?!() + ; }
        ?  && (~~ ( ||  ||  || )) { +: ; }
        ?  { +: (, ) + ; }
        ?  ||  { +: (, ) + ; }
        ?  ||  ||  { +: (, ); }
        ?  { +:  + (, ); }
        * <-.(, ) || ;
    }
}
<=.;

: [, , , , , , , , , , , ];
: [, , , , , , ];

   * {
    ?  ->? [, , , , , , ] {
        * ;
    }
    ?  ->? [, , , ] {
        * ;
    }
    ? () {
        * ;
    }
    * ;
}

  * {
    ?  ---  {
        * ;
    } ,, ?  ---  {
        * ;
    } ,, ? ( --- ) {
        * ;
    } ,, {
        * ;
    }
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<+>>;
    }
}

  * {
    ( ++ );
}

  * {
    ,.,  ++ ;
}

 ? * {
    : ;
    : ;
     <> @@;
    : @@@;
    : <<-..>>;
    : <<>> ++  + <<>> ++ ;
    *  - ;
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<>>;
    }
}

@  {
    => ? ? ? ? ? ? ? ? * {
         <>;
        ? ::  {
            ': @@@;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<..>>;
        } ,, {
             <> ;  <> ;
             <> ;  <> ;  <> ;  <> ;
             <> [, ];
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': [, , , , , , , <<>>, <<>>];
        }
        ? ' ::: [, ] {
            ': (
                ', ', ', ', ', ', '
            );
        } ,, {
            ': ';
        }
    }

    -  * {
        : '() - .();
        :  +++ ;
        : ;
        : ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        == ``  
        * ([, , , , ]);
    }

    ! * {
           * { * <-.((), , ); }
        *  --- {{
             -> ',
             -> (', ),
             -> (', ),
             -> (' + '<<>>, ),
             -> (' + '<<>>, ),
             -> (', ),
             -> (', )
        }};
    }

     * {
        :  +++ ;
        : ;

        : '<<..-..>>;
        : <-.(<<....->>);
        : ;

        : (', ');
        :  + (');

        ...  ->? [, , , , , , ] {
            ++: ;
            +: <<>>;
            : <<..>>;
        }

        -: '<<>> ++  ++  ++ ;
        -: '<<>> ++  ++ ;

        * ;
    }
}

@  {
    =>  * {
        ': ;
    }

    ! * {
           * { * <-.((), , ); }
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : ;
        ?  { +: ?!() + ; }
        ?  && (~~ ( ||  ||  || )) { +: ; }
        ?  { +: (, ) + ; }
        ?  ||  { +: (, ) + ; }
        ?  ||  ||  { +: (, ); }
        ?  { +:  + (, ); }
        * <-.(, ) || ;
    }
}
<=.;

: [, , , , , , , , , , , ];
: [, , , , , , ];

   * {
    ?  ->? [, , , , , , ] {
        * ;
    }
    ?  ->? [, , , ] {
        * ;
    }
    ? () {
        * ;
    }
    * ;
}

  * {
    ?  ---  {
        * ;
    } ,, ?  ---  {
        * ;
    } ,, ? ( --- ) {
        * ;
    } ,, {
        * ;
    }
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<+>>;
    }
}

  * {
    ( ++ );
}

  * {
    ,.,  ++ ;
}

 ? * {
    : ;
    : ;
     <> @@;
    : @@@;
    : <<-..>>;
    : <<>> ++  + <<>> ++ ;
    *  - ;
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<>>;
    }
}

@  {
    => ? ? ? ? ? ? ? ? * {
         <>;
        ? ::  {
            ': @@@;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<..>>;
        } ,, {
             <> ;  <> ;
             <> ;  <> ;  <> ;  <> ;
             <> [, ];
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': [, , , , , , , <<>>, <<>>];
        }
        ? ' ::: [, ] {
            ': (
                ', ', ', ', ', ', '
            );
        } ,, {
            ': ';
        }
    }

    -  * {
        : '() - .();
        :  +++ ;
        : ;
        : ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        == ``  
        * ([, , , , ]);
    }

    ! * {
           * { * <-.((), , ); }
        *  --- {{
             -> ',
             -> (', ),
             -> (', ),
             -> (' + '<<>>, ),
             -> (' + '<<>>, ),
             -> (', ),
             -> (', )
        }};
    }

     * {
        :  +++ ;
        : ;

        : '<<..-..>>;
        : <-.(<<....->>);
        : ;

        : (', ');
        :  + (');

        ...  ->? [, , , , , , ] {
            ++: ;
            +: <<>>;
            : <<..>>;
        }

        -: '<<>> ++  ++  ++ ;
        -: '<<>> ++  ++ ;

        * ;
    }
}

@  {
    =>  * {
        ': ;
    }

    ! * {
           * { * <-.((), , ); }
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : ;
        ?  { +: ?!() + ; }
        ?  && (~~ ( ||  ||  || )) { +: ; }
        ?  { +: (, ) + ; }
        ?  ||  { +: (, ) + ; }
        ?  ||  ||  { +: (, ); }
        ?  { +:  + (, ); }
        * <-.(, ) || ;
    }
}
<=.;

: [, , , , , , , , , , , ];
: [, , , , , , ];

   * {
    ?  ->? [, , , , , , ] {
        * ;
    }
    ?  ->? [, , , ] {
        * ;
    }
    ? () {
        * ;
    }
    * ;
}

  * {
    ?  ---  {
        * ;
    } ,, ?  ---  {
        * ;
    } ,, ? ( --- ) {
        * ;
    } ,, {
        * ;
    }
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<+>>;
    }
}

  * {
    ( ++ );
}

  * {
    ,.,  ++ ;
}

 ? * {
    : ;
    : ;
     <> @@;
    : @@@;
    : <<-..>>;
    : <<>> ++  + <<>> ++ ;
    *  - ;
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<>>;
    }
}

@  {
    => ? ? ? ? ? ? ? ? * {
         <>;
        ? ::  {
            ': @@@;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<..>>;
        } ,, {
             <> ;  <> ;
             <> ;  <> ;  <> ;  <> ;
             <> [, ];
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': [, , , , , , , <<>>, <<>>];
        }
        ? ' ::: [, ] {
            ': (
                ', ', ', ', ', ', '
            );
        } ,, {
            ': ';
        }
    }

    -  * {
        : '() - .();
        :  +++ ;
        : ;
        : ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        == ``  
        * ([, , , , ]);
    }

    ! * {
           * { * <-.((), , ); }
        *  --- {{
             -> ',
             -> (', ),
             -> (', ),
             -> (' + '<<>>, ),
             -> (' + '<<>>, ),
             -> (', ),
             -> (', )
        }};
    }

     * {
        :  +++ ;
        : ;

        : '<<..-..>>;
        : <-.(<<....->>);
        : ;

        : (', ');
        :  + (');

        ...  ->? [, , , , , , ] {
            ++: ;
            +: <<>>;
            : <<..>>;
        }

        -: '<<>> ++  ++  ++ ;
        -: '<<>> ++  ++ ;

        * ;
    }
}

@  {
    =>  * {
        ': ;
    }

    ! * {
           * { * <-.((), , ); }
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : ;
        ?  { +: ?!() + ; }
        ?  && (~~ ( ||  ||  || )) { +: ; }
        ?  { +: (, ) + ; }
        ?  ||  { +: (, ) + ; }
        ?  ||  ||  { +: (, ); }
        ?  { +:  + (, ); }
        * <-.(, ) || ;
    }
}
<=.;

: [, , , , , , , , , , , ];
: [, , , , , , ];

   * {
    ?  ->? [, , , , , , ] {
        * ;
    }
    ?  ->? [, , , ] {
        * ;
    }
    ? () {
        * ;
    }
    * ;
}

  * {
    ?  ---  {
        * ;
    } ,, ?  ---  {
        * ;
    } ,, ? ( --- ) {
        * ;
    } ,, {
        * ;
    }
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<+>>;
    }
}

  * {
    ( ++ );
}

  * {
    ,.,  ++ ;
}

 ? * {
    : ;
    : ;
     <> @@;
    : @@@;
    : <<-..>>;
    : <<>> ++  + <<>> ++ ;
    *  - ;
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<>>;
    }
}

@  {
    => ? ? ? ? ? ? ? ? * {
         <>;
        ? ::  {
            ': @@@;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<..>>;
        } ,, {
             <> ;  <> ;
             <> ;  <> ;  <> ;  <> ;
             <> [, ];
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': [, , , , , , , <<>>, <<>>];
        }
        ? ' ::: [, ] {
            ': (
                ', ', ', ', ', ', '
            );
        } ,, {
            ': ';
        }
    }

    -  * {
        : '() - .();
        :  +++ ;
        : ;
        : ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        == ``  
        * ([, , , , ]);
    }

    ! * {
           * { * <-.((), , ); }
        *  --- {{
             -> ',
             -> (', ),
             -> (', ),
             -> (' + '<<>>, ),
             -> (' + '<<>>, ),
             -> (', ),
             -> (', )
        }};
    }

     * {
        :  +++ ;
        : ;

        : '<<..-..>>;
        : <-.(<<....->>);
        : ;

        : (', ');
        :  + (');

        ...  ->? [, , , , , , ] {
            ++: ;
            +: <<>>;
            : <<..>>;
        }

        -: '<<>> ++  ++  ++ ;
        -: '<<>> ++  ++ ;

        * ;
    }
}

@  {
    =>  * {
        ': ;
    }

    ! * {
           * { * <-.((), , ); }
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : ;
        ?  { +: ?!() + ; }
        ?  && (~~ ( ||  ||  || )) { +: ; }
        ?  { +: (, ) + ; }
        ?  ||  { +: (, ) + ; }
        ?  ||  ||  { +: (, ); }
        ?  { +:  + (, ); }
        * <-.(, ) || ;
    }
}
<=.;

: [, , , , , , , , , , , ];
: [, , , , , , ];

   * {
    ?  ->? [, , , , , , ] {
        * ;
    }
    ?  ->? [, , , ] {
        * ;
    }
    ? () {
        * ;
    }
    * ;
}

  * {
    ?  ---  {
        * ;
    } ,, ?  ---  {
        * ;
    } ,, ? ( --- ) {
        * ;
    } ,, {
        * ;
    }
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<+>>;
    }
}

  * {
    ( ++ );
}

  * {
    ,.,  ++ ;
}

 ? * {
    : ;
    : ;
     <> @@;
    : @@@;
    : <<-..>>;
    : <<>> ++  + <<>> ++ ;
    *  - ;
}

  * {
    ?  >: $ {
        * ;
    } ,, ? < {
        * ;
    } ,, {
        * <<>>;
    }
}

@  {
    => ? ? ? ? ? ? ? ? * {
         <>;
        ? ::  {
            ': @@@;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<>>;
            ': '<<..>>;
        } ,, {
             <> ;  <> ;
             <> ;  <> ;  <> ;  <> ;
             <> [, ];
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': ;
            ': [, , , , , , , <<>>, <<>>];
        }
        ? ' ::: [, ] {
            ': (
                ', ', ', ', ', ', '
            );
        } ,, {
            ': ';
        }
    }

    -  * {
        : '() - .();
        :  +++ ;
        : ;
        : ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        :  --- ; --: ;
        == ``  
        * ([, , , , ]);
    }

    ! * {
           * { * <-.((), , ); }
        *  --- {{
             -> ',
             -> (', ),
             -> (', ),
             -> (' + '<<>>, ),
             -> (' + '<<>>, ),
             -> (', ),
             -> (', )
        }};
    }

     * {
        :  +++ ;
        : ;

        : '<<..-..>>;
        : <-.(<<....->>);
        : ;

        : (', ');
        :  + (');

        ...  ->? [, , , , , , ] {
            ++: ;
            +: <<>>;
            : <<..>>;
        }

        -: '<<>> ++  ++  ++ ;
        -: '<<>> ++  ++ ;

        * ;
    }
}

@  {
    =>  * {
        ': ;
    }

    ! * {
           * { * <-.((), , ); }
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : '<<>>;
        : ;
        ?  { +: ?!() + ; }
        ?  && (~~ ( ||  ||  || )) { +: ; }
        ?  { +: (, ) + ; }
        ?  ||  { +: (, ) + ; }
        ?  ||  ||  { +: (, ); }
        ?  { +:  + (, ); }
        * <-.(, ) || ;
    }
}
